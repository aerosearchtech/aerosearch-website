# SHA-256 hex of the /demo gate password. Leave unset to use the built-in hash.
# node -e "console.log(require('crypto').createHash('sha256').update('YOUR_PASSWORD').digest('hex'))"
NEXT_PUBLIC_DEMO_PASSWORD_SHA256=
